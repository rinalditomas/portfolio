# Static site — no build step needed.
# An explicit (no-op) command + publish dir so this file fully overrides the
# leftover Create React App settings in the Netlify UI, which still expected a
# `build/` directory that no longer exists.
[build]
  command = "echo 'No build step — static site.'"
  publish = "."
